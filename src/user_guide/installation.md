# Installation

You only need to clone Gtheme's repository, run the installation script and follow the initial setup

```console
git clone --depth 1 https://github.com/daavidrgz/gtheme.git gtheme 
cd gtheme
./install.sh
```